<div class="w-full bg-gray-50 dark:bg-neutral-950 border-b dark:border-b-neutral-800 px-8 py-4 mb-6 flex flex-row items-center">
    <span>Gradeventus</span>
    <div class="flex-grow"></div>
    <div {{ stimulus_controller('theme') }}>
        <button {{ stimulus_action('theme', 'toggle', 'click') }} class="btn">
            Changer de thème : <span id="theme"></span>
        </button>
    </div>
</div>